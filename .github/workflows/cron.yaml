name: Hourly cron job
on:
  schedule:
    - cron: '*/60 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hourly cron job
        run: |
          curl --request GET \
          --url 'https://test-project-6k0bstoal-henrytech4365-gmailcom.vercel.app/api/crawl' \
